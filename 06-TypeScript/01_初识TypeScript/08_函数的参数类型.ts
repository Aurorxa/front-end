/**
 * 求和
 * @param num1 {number} 数值类型
 * @param num2 {number} 数值类型
 * @returns 
 */
function sum(num1: number, num2: number) {
  return num1 + num2
}

let result = sum(1, 2)
console.log(result)

export { }